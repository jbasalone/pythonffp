# To count how many times we execute a loop, we introduce a counter variable that
# starts at 0 and we add one to it each time through the loop
# Finish the loop counter

counter = 0
print 'Starting at', counter
for n in []:
   counter = counter + 1
   print counter, n
print "Total Count: ", counter
